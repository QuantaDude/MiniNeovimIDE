# nvim-minimal (Neovim 0.12+)

A **minimal, explicit Neovim configuration** to experiment with **Neovim 0.12 nightly** features:

- native package manager (`vim.pack`)
- native LSP (`vim.lsp.start`)
- Tree-sitter
- `mini.files` as the file explorer

No plugin manager frameworks.  
No abstractions.  
No hidden magic.

---

## Features

- 🚀 Neovim **0.12+ nightly**
- 📦 Native package manager (`vim.pack`)
- 🌳 Tree-sitter for syntax parsing
- 🔌 Native LSP (no `nvim-lspconfig` dependency)
- 📁 File explorer via `mini.files`

## Dependencies

- Neovim 0.12.0
- lua-language-server
- ripgrep
- typescript-language-server
- clangd
- pyright

